{
  "name": "vibebox",
  "version": "0.1.10",
  "description": "Local-first active user pattern graph and blackbox memory middleware for AI coding agents.",
  "type": "module",
  "bin": {
    "vibebox": "./bin/vibebox.mjs"
  },
  "scripts": {
    "sync:plugin": "node scripts/sync-plugin-package.mjs",
    "test": "node --test",
    "check": "node --check bin/vibebox.mjs && node --check src/core.mjs && node --check src/cli.mjs && node --check scripts/claude-vibebox-hook.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
