{
  "name": "vibebox",
  "version": "0.1.0",
  "description": "Local-first memory middleware for AI coding sessions.",
  "type": "module",
  "bin": {
    "vibebox": "./bin/vibebox.mjs"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check bin/vibebox.mjs && node --check src/core.mjs && node --check src/cli.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
